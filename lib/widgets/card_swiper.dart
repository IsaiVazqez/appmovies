import 'package:flutter/material.dart';

class CartSwipe extends StatelessWidget {
  const CartSwipe({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    final size = MediaQuery.of(context);
    return Container(
      width: double.infinity,
      height: 500,
      color: Colors.indigo,
    );
  }
}
